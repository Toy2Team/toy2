import { atom } from 'recoil';

export const firstInputState = atom({
  key: 'firstInputState',
  default: '',
});

export const secondInputState = atom({
  key: 'secondInputState',
  default: '',
});

export const memoInputState = atom({
  key: 'memoInputState',
  default: '',
});

export const startInputState = atom({
  key: 'startInputState',
  default: '',
});

export const endInputState = atom({
  key: 'endInputState',
  default: '',
});
